diff --git a/ocs_ci/helpers/helpers.py b/ocs_ci/helpers/helpers.py
index 465f1434b..37183d5ef 100644
--- a/ocs_ci/helpers/helpers.py
+++ b/ocs_ci/helpers/helpers.py
@@ -132,7 +132,7 @@ def apply_resource(**kwargs):
     return ocs_obj
 
 
-def wait_for_resource_state(resource, state, timeout=60):
+def wait_for_resource_state(resource, state, timeout=120):
     """
     Wait for a resource to get to a given status
 
@@ -1728,7 +1728,7 @@ def create_build_from_docker_image(
         oc_cmd += f"--kubeconfig {kubeconfig} "
     oc_cmd += command
     logger.info(f"Running command {oc_cmd}")
-    result = run(oc_cmd, stdout=PIPE, stderr=PIPE, timeout=15, shell=True)
+    result = run(oc_cmd, stdout=PIPE, stderr=PIPE, timeout=15, shell=True, executable="/bin/bash")
     if result.stderr.decode():
         raise UnavailableBuildException(
             f"Build creation failed with error: {result.stderr.decode()}"
@@ -2195,6 +2195,14 @@ def change_default_storageclass(scname):
     """
     default_sc = get_default_storage_class()
     ocp_obj = ocp.OCP(kind="StorageClass")
+    # Change the new storageclass to default
+    patch = (
+        ' \'{"metadata": {"annotations":'
+        '{"storageclass.kubernetes.io/is-default-class"'
+        ':"true"}}}\' '
+    )
+    patch_cmd = f"patch storageclass {scname} -p" + patch
+    ocp_obj.exec_oc_cmd(command=patch_cmd)
     if default_sc:
         # Change the existing default Storageclass annotation to false
         for sc in default_sc:
@@ -2206,17 +2214,6 @@ def change_default_storageclass(scname):
             patch_cmd = f"patch storageclass {sc} -p" + patch
             ocp_obj.exec_oc_cmd(command=patch_cmd)
 
-    # Change the new storageclass to default
-    patch = (
-        ' \'{"metadata": {"annotations":'
-        '{"storageclass.kubernetes.io/is-default-class"'
-        ':"true"}}}\' '
-    )
-    patch_cmd = f"patch storageclass {scname} -p" + patch
-    ocp_obj.exec_oc_cmd(command=patch_cmd)
-    return True
-
-
 def is_volume_present_in_backend(interface, image_uuid, pool_name=None):
     """
     Check whether Image/Subvolume is present in the backend.
@@ -7658,6 +7655,23 @@ def delete_cephfs_ec_pool(pool_name):
     full_pool_name = f"{fs_name}-{pool_name}"
 
     ct_pod = pod.get_ceph_tools_pod()
+    ct_pod.exec_ceph_cmd(
+            f"ceph fs rm_data_pool {fs_name} {full_pool_name}",
+            format=None,
+        )
+    logger.info(f"Removed '{full_pool_name}' from CephFS fsmap")
+    ct_pod.exec_ceph_cmd(
+            f"ceph config set mon mon_allow_pool_delete true",
+            format=None,
+    )
+    ct_pod.exec_ceph_cmd(
+            f"ceph osd pool delete {full_pool_name} {full_pool_name} "
+            f"--yes-i-really-really-mean-it",
+            format=None,
+    )
+    logger.info(f"Ceph pool '{full_pool_name}' deleted directly")
+    cephfs_name = get_cephfs_name()
+    ct_pod.exec_ceph_cmd(f"ceph fs rm_data_pool {cephfs_name} {full_pool_name}")
     logger.info(f"Waiting for Ceph pool '{full_pool_name}' to be removed")
     try:
         for pools in TimeoutSampler(600, 15, ct_pod.exec_ceph_cmd, "ceph osd pool ls"):
